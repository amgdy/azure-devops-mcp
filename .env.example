# Azure DevOps MCP Server - Environment Variables Configuration
# Copy this file to .env and customize for your environment

# Required: Azure DevOps organization name
AZURE_DEVOPS_ORG=myorg

# Optional: Azure tenant ID for multi-tenant scenarios
# AZURE_TENANT_ID=your-tenant-id

# Optional: Azure token credentials mode (default: "dev")
# AZURE_TOKEN_CREDENTIALS=dev

# Remote server configuration
REMOTE_PORT=3000

# CORS configuration (comma-separated)
# ALLOWED_ORIGINS=https://myapp.com,https://localhost:3000

# DNS rebinding protection (comma-separated)  
# ALLOWED_HOSTS=myapp.com,localhost

# Example for Azure Container Apps
# The following environment variables should be configured in your container app:
# - AZURE_DEVOPS_ORG: Your Azure DevOps organization name
# - AZURE_CLIENT_ID: Managed identity client ID (if using managed identity)
# - AZURE_CLIENT_SECRET: Service principal secret (if using service principal)
# - AZURE_TENANT_ID: Azure tenant ID

# Development/Testing
# For local testing, you can set AZURE_TOKEN_CREDENTIALS=dev to use Azure CLI authentication