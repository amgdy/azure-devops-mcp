# Environment variables for Docker deployment
# Copy this file to .env and fill in your values

# Required: Azure DevOps organization name
ADO_ORG=contoso

# Required: Azure DevOps Personal Access Token
# Generate from: https://dev.azure.com/{your-org}/_usersSettings/tokens
ADO_PAT=your-personal-access-token-here

# Optional: Server port (default: 3000)
# SERVER_PORT=3000

# Optional: Node environment
# NODE_ENV=production

# Optional: Enable specific domains (default: all)
# Uncomment and modify as needed:
# ADO_DOMAINS=repositories,builds,work

# Optional: Azure tenant ID (for multi-tenant scenarios)
# AZURE_TENANT_ID=your-tenant-id-here